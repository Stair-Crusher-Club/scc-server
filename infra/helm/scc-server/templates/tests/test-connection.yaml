apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scc-server.fullname" . }}-test-connection"
  labels:
    {{- include "scc-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scc-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
