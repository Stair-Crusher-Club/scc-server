apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "amazon-eks-pod-identity-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "amazon-eks-pod-identity-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "amazon-eks-pod-identity-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
